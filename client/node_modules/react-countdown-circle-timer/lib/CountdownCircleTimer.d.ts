/// <reference types="react" />
import { useCountdown } from '@countdown/shared';
import type { Props } from './types';
declare const CountdownCircleTimer: {
    (props: Props): JSX.Element;
    displayName: string;
};
export { CountdownCircleTimer, useCountdown };
